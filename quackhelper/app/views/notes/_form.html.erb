<%= bootstrap_form_with(model: @note, local: true, url: event_notes_url) do |form| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%=# form.text_field :Event_id, id: :note_Events_id
    %>
    <% eary = Array.new %>
    <% Event.all.each do |e| eary.push(e.id) end %>
    
    <%= form.select :event_id, eary %>
  </div>

  <div class="field">
    <% uary = Array.new %>
    <% User.all.each do |e| uary.push(e.name) end %>
    <%= form.select :user_id, uary %>
  </div>

  <div class="field">
    <%= form.text_area :description, id: :note_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
